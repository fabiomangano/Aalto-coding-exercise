export type User = {
  email: string,
  id: string,
  name: string,
  phone: string,
  username: string,
  website: string,
}

export type Todo = {
  id: number;
  title: string;
  completed: string;
  userId: number;
}